import React from 'react'
import 
const Navbar = ({activeMenu}) => {
  return (
    <div>
        <button className=''
        onClick={()=> [
            setOpenSideMenu(!setOpenSideMenu);
        ]}
      >
        {setOpenSideMenu ? (
            
        ) : (

        )}
        </button>
    </div>
  )
}

export default Navbar