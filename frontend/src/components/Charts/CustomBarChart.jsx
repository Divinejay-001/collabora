import React from 'react'
import {
    BarChart,
    Bar,
    XAxis,
    YAxis,
    Tooltip,
    CartesianGrid,
    ResponsiveContainer,
    Cell
} from 'recharts'

const CustomBarChart = ({data}) => {
  return (
    <div>CustomBarChart</div>
  )
}

export default CustomBarChart